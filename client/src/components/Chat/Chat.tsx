import { FunctionComponent } from "react";
import "./chat.scss";

interface ChatProps {
  message?: string;
}

export const Chat: FunctionComponent<ChatProps> = (): JSX.Element => {
  return <></>;
};
